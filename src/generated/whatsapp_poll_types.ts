// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: whatsapp_poll_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp.js";

export const protobufPackage = "photon.whatsapp.v1";

export interface PollChoice {
  index: number;
  text: string;
  voteCount: number;
}

export interface Poll {
  pollId: string;
  question: string;
  choices: PollChoice[];
  allowMultipleChoices: boolean;
  hideVoterNames: boolean;
}

export interface PollChangeEvent {
  recipient: string;
  pollId: string;
  occurredAt: Date | undefined;
  isFromMe: boolean;
  created?: Poll | undefined;
  updated?: Poll | undefined;
  voteChanged?: Poll | undefined;
  choicesChanged?: Poll | undefined;
}

function createBasePollChoice(): PollChoice {
  return { index: 0, text: "", voteCount: 0 };
}

export const PollChoice: MessageFns<PollChoice> = {
  encode(message: PollChoice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.voteCount !== 0) {
      writer.uint32(24).int32(message.voteCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PollChoice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePollChoice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.voteCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PollChoice {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      voteCount: isSet(object.voteCount)
        ? globalThis.Number(object.voteCount)
        : isSet(object.vote_count)
        ? globalThis.Number(object.vote_count)
        : 0,
    };
  },

  toJSON(message: PollChoice): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.voteCount !== 0) {
      obj.voteCount = Math.round(message.voteCount);
    }
    return obj;
  },

  create(base?: DeepPartial<PollChoice>): PollChoice {
    return PollChoice.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PollChoice>): PollChoice {
    const message = createBasePollChoice();
    message.index = object.index ?? 0;
    message.text = object.text ?? "";
    message.voteCount = object.voteCount ?? 0;
    return message;
  },
};

function createBasePoll(): Poll {
  return { pollId: "", question: "", choices: [], allowMultipleChoices: false, hideVoterNames: false };
}

export const Poll: MessageFns<Poll> = {
  encode(message: Poll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pollId !== "") {
      writer.uint32(10).string(message.pollId);
    }
    if (message.question !== "") {
      writer.uint32(18).string(message.question);
    }
    for (const v of message.choices) {
      PollChoice.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.allowMultipleChoices !== false) {
      writer.uint32(32).bool(message.allowMultipleChoices);
    }
    if (message.hideVoterNames !== false) {
      writer.uint32(40).bool(message.hideVoterNames);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Poll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePoll();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pollId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.question = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.choices.push(PollChoice.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.allowMultipleChoices = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.hideVoterNames = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Poll {
    return {
      pollId: isSet(object.pollId)
        ? globalThis.String(object.pollId)
        : isSet(object.poll_id)
        ? globalThis.String(object.poll_id)
        : "",
      question: isSet(object.question) ? globalThis.String(object.question) : "",
      choices: globalThis.Array.isArray(object?.choices) ? object.choices.map((e: any) => PollChoice.fromJSON(e)) : [],
      allowMultipleChoices: isSet(object.allowMultipleChoices)
        ? globalThis.Boolean(object.allowMultipleChoices)
        : isSet(object.allow_multiple_choices)
        ? globalThis.Boolean(object.allow_multiple_choices)
        : false,
      hideVoterNames: isSet(object.hideVoterNames)
        ? globalThis.Boolean(object.hideVoterNames)
        : isSet(object.hide_voter_names)
        ? globalThis.Boolean(object.hide_voter_names)
        : false,
    };
  },

  toJSON(message: Poll): unknown {
    const obj: any = {};
    if (message.pollId !== "") {
      obj.pollId = message.pollId;
    }
    if (message.question !== "") {
      obj.question = message.question;
    }
    if (message.choices?.length) {
      obj.choices = message.choices.map((e) => PollChoice.toJSON(e));
    }
    if (message.allowMultipleChoices !== false) {
      obj.allowMultipleChoices = message.allowMultipleChoices;
    }
    if (message.hideVoterNames !== false) {
      obj.hideVoterNames = message.hideVoterNames;
    }
    return obj;
  },

  create(base?: DeepPartial<Poll>): Poll {
    return Poll.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Poll>): Poll {
    const message = createBasePoll();
    message.pollId = object.pollId ?? "";
    message.question = object.question ?? "";
    message.choices = object.choices?.map((e) => PollChoice.fromPartial(e)) || [];
    message.allowMultipleChoices = object.allowMultipleChoices ?? false;
    message.hideVoterNames = object.hideVoterNames ?? false;
    return message;
  },
};

function createBasePollChangeEvent(): PollChangeEvent {
  return {
    recipient: "",
    pollId: "",
    occurredAt: undefined,
    isFromMe: false,
    created: undefined,
    updated: undefined,
    voteChanged: undefined,
    choicesChanged: undefined,
  };
}

export const PollChangeEvent: MessageFns<PollChangeEvent> = {
  encode(message: PollChangeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recipient !== "") {
      writer.uint32(10).string(message.recipient);
    }
    if (message.pollId !== "") {
      writer.uint32(18).string(message.pollId);
    }
    if (message.occurredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.occurredAt), writer.uint32(26).fork()).join();
    }
    if (message.isFromMe !== false) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.created !== undefined) {
      Poll.encode(message.created, writer.uint32(82).fork()).join();
    }
    if (message.updated !== undefined) {
      Poll.encode(message.updated, writer.uint32(90).fork()).join();
    }
    if (message.voteChanged !== undefined) {
      Poll.encode(message.voteChanged, writer.uint32(98).fork()).join();
    }
    if (message.choicesChanged !== undefined) {
      Poll.encode(message.choicesChanged, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PollChangeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePollChangeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recipient = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pollId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.occurredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.created = Poll.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.updated = Poll.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.voteChanged = Poll.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.choicesChanged = Poll.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PollChangeEvent {
    return {
      recipient: isSet(object.recipient) ? globalThis.String(object.recipient) : "",
      pollId: isSet(object.pollId)
        ? globalThis.String(object.pollId)
        : isSet(object.poll_id)
        ? globalThis.String(object.poll_id)
        : "",
      occurredAt: isSet(object.occurredAt)
        ? fromJsonTimestamp(object.occurredAt)
        : isSet(object.occurred_at)
        ? fromJsonTimestamp(object.occurred_at)
        : undefined,
      isFromMe: isSet(object.isFromMe)
        ? globalThis.Boolean(object.isFromMe)
        : isSet(object.is_from_me)
        ? globalThis.Boolean(object.is_from_me)
        : false,
      created: isSet(object.created) ? Poll.fromJSON(object.created) : undefined,
      updated: isSet(object.updated) ? Poll.fromJSON(object.updated) : undefined,
      voteChanged: isSet(object.voteChanged)
        ? Poll.fromJSON(object.voteChanged)
        : isSet(object.vote_changed)
        ? Poll.fromJSON(object.vote_changed)
        : undefined,
      choicesChanged: isSet(object.choicesChanged)
        ? Poll.fromJSON(object.choicesChanged)
        : isSet(object.choices_changed)
        ? Poll.fromJSON(object.choices_changed)
        : undefined,
    };
  },

  toJSON(message: PollChangeEvent): unknown {
    const obj: any = {};
    if (message.recipient !== "") {
      obj.recipient = message.recipient;
    }
    if (message.pollId !== "") {
      obj.pollId = message.pollId;
    }
    if (message.occurredAt !== undefined) {
      obj.occurredAt = message.occurredAt.toISOString();
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.created !== undefined) {
      obj.created = Poll.toJSON(message.created);
    }
    if (message.updated !== undefined) {
      obj.updated = Poll.toJSON(message.updated);
    }
    if (message.voteChanged !== undefined) {
      obj.voteChanged = Poll.toJSON(message.voteChanged);
    }
    if (message.choicesChanged !== undefined) {
      obj.choicesChanged = Poll.toJSON(message.choicesChanged);
    }
    return obj;
  },

  create(base?: DeepPartial<PollChangeEvent>): PollChangeEvent {
    return PollChangeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PollChangeEvent>): PollChangeEvent {
    const message = createBasePollChangeEvent();
    message.recipient = object.recipient ?? "";
    message.pollId = object.pollId ?? "";
    message.occurredAt = object.occurredAt ?? undefined;
    message.isFromMe = object.isFromMe ?? false;
    message.created = (object.created !== undefined && object.created !== null)
      ? Poll.fromPartial(object.created)
      : undefined;
    message.updated = (object.updated !== undefined && object.updated !== null)
      ? Poll.fromPartial(object.updated)
      : undefined;
    message.voteChanged = (object.voteChanged !== undefined && object.voteChanged !== null)
      ? Poll.fromPartial(object.voteChanged)
      : undefined;
    message.choicesChanged = (object.choicesChanged !== undefined && object.choicesChanged !== null)
      ? Poll.fromPartial(object.choicesChanged)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
