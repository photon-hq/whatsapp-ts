// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: whatsapp_profile_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "photon.whatsapp.v1";

export interface ModifyProfileRequest {
  /** New push name. Must be non-empty after trimming when present. */
  name?:
    | string
    | undefined;
  /** New about text. Empty string clears the text status when WhatsApp allows it. */
  about?:
    | string
    | undefined;
  /** New avatar image bytes (JPEG or PNG). Staged to disk before the helper call. */
  avatar?: Uint8Array | undefined;
}

export interface ModifyProfileResponse {
  nameUpdated: boolean;
  aboutUpdated: boolean;
  avatarUpdated: boolean;
}

function createBaseModifyProfileRequest(): ModifyProfileRequest {
  return { name: undefined, about: undefined, avatar: undefined };
}

export const ModifyProfileRequest: MessageFns<ModifyProfileRequest> = {
  encode(message: ModifyProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined) {
      writer.uint32(10).string(message.name);
    }
    if (message.about !== undefined) {
      writer.uint32(18).string(message.about);
    }
    if (message.avatar !== undefined) {
      writer.uint32(26).bytes(message.avatar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifyProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifyProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.about = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.avatar = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModifyProfileRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      about: isSet(object.about) ? globalThis.String(object.about) : undefined,
      avatar: isSet(object.avatar) ? bytesFromBase64(object.avatar) : undefined,
    };
  },

  toJSON(message: ModifyProfileRequest): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.about !== undefined) {
      obj.about = message.about;
    }
    if (message.avatar !== undefined) {
      obj.avatar = base64FromBytes(message.avatar);
    }
    return obj;
  },

  create(base?: DeepPartial<ModifyProfileRequest>): ModifyProfileRequest {
    return ModifyProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifyProfileRequest>): ModifyProfileRequest {
    const message = createBaseModifyProfileRequest();
    message.name = object.name ?? undefined;
    message.about = object.about ?? undefined;
    message.avatar = object.avatar ?? undefined;
    return message;
  },
};

function createBaseModifyProfileResponse(): ModifyProfileResponse {
  return { nameUpdated: false, aboutUpdated: false, avatarUpdated: false };
}

export const ModifyProfileResponse: MessageFns<ModifyProfileResponse> = {
  encode(message: ModifyProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nameUpdated !== false) {
      writer.uint32(8).bool(message.nameUpdated);
    }
    if (message.aboutUpdated !== false) {
      writer.uint32(16).bool(message.aboutUpdated);
    }
    if (message.avatarUpdated !== false) {
      writer.uint32(24).bool(message.avatarUpdated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifyProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifyProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nameUpdated = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.aboutUpdated = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.avatarUpdated = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModifyProfileResponse {
    return {
      nameUpdated: isSet(object.nameUpdated)
        ? globalThis.Boolean(object.nameUpdated)
        : isSet(object.name_updated)
        ? globalThis.Boolean(object.name_updated)
        : false,
      aboutUpdated: isSet(object.aboutUpdated)
        ? globalThis.Boolean(object.aboutUpdated)
        : isSet(object.about_updated)
        ? globalThis.Boolean(object.about_updated)
        : false,
      avatarUpdated: isSet(object.avatarUpdated)
        ? globalThis.Boolean(object.avatarUpdated)
        : isSet(object.avatar_updated)
        ? globalThis.Boolean(object.avatar_updated)
        : false,
    };
  },

  toJSON(message: ModifyProfileResponse): unknown {
    const obj: any = {};
    if (message.nameUpdated !== false) {
      obj.nameUpdated = message.nameUpdated;
    }
    if (message.aboutUpdated !== false) {
      obj.aboutUpdated = message.aboutUpdated;
    }
    if (message.avatarUpdated !== false) {
      obj.avatarUpdated = message.avatarUpdated;
    }
    return obj;
  },

  create(base?: DeepPartial<ModifyProfileResponse>): ModifyProfileResponse {
    return ModifyProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifyProfileResponse>): ModifyProfileResponse {
    const message = createBaseModifyProfileResponse();
    message.nameUpdated = object.nameUpdated ?? false;
    message.aboutUpdated = object.aboutUpdated ?? false;
    message.avatarUpdated = object.avatarUpdated ?? false;
    return message;
  },
};

export type ProfileServiceDefinition = typeof ProfileServiceDefinition;
export const ProfileServiceDefinition = {
  name: "ProfileService",
  fullName: "photon.whatsapp.v1.ProfileService",
  methods: {
    modifyProfile: {
      name: "ModifyProfile",
      requestType: ModifyProfileRequest as typeof ModifyProfileRequest,
      requestStream: false,
      responseType: ModifyProfileResponse as typeof ModifyProfileResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ProfileServiceImplementation<CallContextExt = {}> {
  modifyProfile(
    request: ModifyProfileRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ModifyProfileResponse>>;
}

export interface ProfileServiceClient<CallOptionsExt = {}> {
  modifyProfile(
    request: DeepPartial<ModifyProfileRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ModifyProfileResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
